import { API_ENDPOINTS } from "@/utils/constants";

export const endpoints = {
  humanizer: {
    generate: API_ENDPOINTS.humanizer.generate,
  },
  summarizer: {
    generate: API_ENDPOINTS.summarizer.generate,
  },
};
